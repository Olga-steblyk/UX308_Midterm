import { gymmembership } from '../gym_member.js'

describe("A suite", function() {
    it("contains spec with an expectation", function() {
      let sHello = gymmembership(0);
      expect(sHello).toBe("Thank you for referring a friend, your new cost is $95");
    });
  });

describe("A suite", function() {
    it("contains spec with an expectation", function() {
      let sHello = gymmembership(1);
      expect(sHello).toBe("Thank you for referring a friend, your new cost is $95");
    });
  });
describe("A suite", function() {
    it("contains spec with an expectation", function() {
      let sHello = gymmembership(2);
      expect(sHello).toBe("Thank you for referring a friend, your new cost is $95");
    });
  });

 

  
  